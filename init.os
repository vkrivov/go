Перем ТекущийКаталог;
ТекущийКаталог = ТекущийКаталог();

Путь = ОбъединитьПути(
	ТекущийКаталог,
	"sonar"
);
СоздатьКаталог(Путь);

Путь = ОбъединитьПути(
	ТекущийКаталог,
	"logs"
);
СоздатьКаталог(Путь);

Путь = ОбъединитьПути(
	ТекущийКаталог,
	"git"
);
СоздатьКаталог(Путь);

СтрокаЗапуска = "git init";
ЗапуститьПриложение(СтрокаЗапуска, Путь, Истина);
	